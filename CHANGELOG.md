# Changelog

## v2026.8.12

- 为 ClawHub 版本页补写 2026.8.11 的发布说明（此前漏填 `--changelog`）。

## v2026.8.11

- **修复消息顺序错乱**：修复 `renumberSeq` 中 `?` 占位符与命名参数混用导致 `seq` 未实际更新的问题，并对历史重复 `seq` 数据执行重排。
- **消息浏览改为双模式**：默认倒序（最新消息在底部）；点击「▲ 顶部」切换为正序，从最早消息开始浏览，向下滚动加载更新消息。后端 `getMessages` 新增 `afterSeq` 正序分页参数并透传路由。
- **优化顶部跳转按钮**：改成圆角药丸样式（`▲ 顶部`），固定在「复制全文」按钮上方。
- **README 新增效果预览图**：补充会话列表与会话详情两张截图。

## v2026.8.10

- 移除手动同步按钮与 `/api/sync` 接口；列表请求即近实时同步会话元数据，点进详情即同步该会话内容，5 分钟定时全量兜底已覆盖原手动同步能力，UI 更精简。

## v2026.8.9

- 重写 README 目的与价值章节。
- 列表/Agent 名称过长时显示省略号。
- 详情页 SSR 降级兜底。
- 复制全文/导出增加 10 MB 字节上限与截断提示。
- 如实记录 iframe 沙盒竞态（根因在 OpenClaw 主程序，未根治）。
