# Changelog

## v2026.8.13-beta.2

- **会话详情 UX 重排**：日期/思考/工具/清空与搜索框合并为一行；右下角改为可折叠的四按钮悬浮组（思考/工具/顶部/复制全文），展开图标默认半透明、仅 hover 显形。
- **搜索增强**：尊重工具/思考开关（隐藏状态下不检索工具与思考消息）；搜索高亮时显示被隐藏的工具/思考消息并自动展开折叠详情。
- **日期控件优化**：清空按钮移至搜索框右侧、胶囊形 + 固定槽位宽度避免头部变形；日期错误提示改为前端 toast 并贴字段下方，不再外溢到列表页。
- **细节修复**：修复 toast 因 CSS 括号失衡未生效；搜索栏底色统一为 meta 行同色；关闭详情时彻底重置搜索/日期/消息状态。

## v2026.8.13-beta.1

- **预发布**：核心 `2026.8.13` 的预发布版本，功能与 `2026.8.13` 一致。
- **隐私修复**：会话详情预览截图对泄露的真实姓名打马赛克，避免发布到 ClawHub 后暴露个人身份信息。
- **README 重构**：按规范精简为「解决什么问题 + 怎么用」，补全英文版与顶部语言切换，效果预览图移至 `docs/screenshots/`。
- 新增会话列表（多渠道聚合、按人归一）与会话详情（时间线 + 工具卡）两张效果预览截图。

## v2026.8.12

- 为 ClawHub 版本页补写 2026.8.11 的发布说明（此前漏填 `--changelog`）。

## v2026.8.11

- **修复消息顺序错乱**：修复 `renumberSeq` 中 `?` 占位符与命名参数混用导致 `seq` 未实际更新的问题，并对历史重复 `seq` 数据执行重排。
- **消息浏览改为双模式**：默认倒序（最新消息在底部）；点击「▲ 顶部」切换为正序，从最早消息开始浏览，向下滚动加载更新消息。后端 `getMessages` 新增 `afterSeq` 正序分页参数并透传路由。
- **优化顶部跳转按钮**：改成圆角药丸样式（`▲ 顶部`），固定在「复制全文」按钮上方。
- **README 新增效果预览图**：补充会话列表与会话详情两张截图。

## v2026.8.10

- 移除手动同步按钮与 `/api/sync` 接口；列表请求即近实时同步会话元数据，点进详情即同步该会话内容，5 分钟定时全量兜底已覆盖原手动同步能力，UI 更精简。

## v2026.8.9

- 重写 README 目的与价值章节。
- 列表/Agent 名称过长时显示省略号。
- 详情页 SSR 降级兜底。
- 复制全文/导出增加 10 MB 字节上限与截断提示。
- 如实记录 iframe 沙盒竞态（根因在 OpenClaw 主程序，未根治）。
